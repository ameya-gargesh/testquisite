<%= form_for @case_type, :html => { :class => "form-horizontal case_type" } do |f| %>

    <% if @case_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@case_type.errors.count, "error") %> prohibited this case_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @case_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@case_type[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :label, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :label, :class => 'form-control' %>
    </div>
    <%= error_span(@case_type[:label]) %>
  </div>
  <div class="control-group">
    <%= f.label :display_order, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :display_order, :class => 'form-control' %>
    </div>
    <%= error_span(@case_type[:display_order]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            case_types_path, :class => 'btn btn-default' %>

<% end %>
